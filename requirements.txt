# pixeliar — Dependencies
# Heavy ML deps are installed on-demand in main.py
# This file lists the essential bootstrap deps

# Google Drive API v3
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
gdown>=5.1.0

# Image processing
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Progress
tqdm>=4.65.0

# ML (PyTorch — Colab has torch pre-installed, pin version)
torch>=2.1.0
torchvision>=0.16.0
